---
stages:
  - build

build:
  stage: build
  image: python:2
  before_script:
    - pip install -U platformio
  script:
    - platformio ci --project-conf platformio.ini src lib
  artifacts:
    paths: [lib]

# vim: set ai et sw=2 ts=2:
